import { createFileRoute } from "@tanstack/react-router"; import { SectionHub } from "@/pages/Platform"; export const Route=createFileRoute("/employee")({component:()=> <SectionHub section="employee"/>});
