import { createFileRoute } from "@tanstack/react-router"; import GlobalDiscovery from "@/pages/GlobalDiscovery"; export const Route=createFileRoute("/countries")({component:()=> <GlobalDiscovery kind="countries"/>});