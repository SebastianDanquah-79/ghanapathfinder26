import { createFileRoute } from "@tanstack/react-router"; import { SectionHub } from "@/pages/Platform"; export const Route=createFileRoute("/classification-center" as any)({component:()=> <SectionHub section="classification"/>});
