import { createFileRoute } from "@tanstack/react-router"; import GlobalDiscovery from "@/pages/GlobalDiscovery"; export const Route=createFileRoute("/build-in-africa")({component:()=> <GlobalDiscovery kind="build"/>});