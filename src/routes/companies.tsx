import { createFileRoute } from "@tanstack/react-router"; import GlobalDiscovery from "@/pages/GlobalDiscovery"; export const Route=createFileRoute("/companies")({component:()=> <GlobalDiscovery kind="companies"/>});