import { createFileRoute } from "@tanstack/react-router"; import { SectionHub } from "@/pages/Platform"; export const Route=createFileRoute("/startup")({component:()=> <SectionHub section="startup"/>});
