import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
export const Route=createFileRoute("/ai")({component:AIAdvisor});
function AIAdvisor(){const [question,setQuestion]=useState("");const [answer,setAnswer]=useState("");const [busy,setBusy]=useState(false);const ask=async()=>{if(!question.trim())return;setBusy(true);setAnswer("");try{const res=await fetch("/api/chat",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({messages:[{id:crypto.randomUUID(),role:"user",parts:[{type:"text",text:question.trim()}]}],context:"Use verified GhanaPathFinder data when available. Never invent university, programme, scholarship, job or deadline facts."})});const body=await res.text();setAnswer(res.ok?body:body||"The AI advisor could not answer that.");}catch{setAnswer("The AI advisor is temporarily unavailable. Please try again.");}finally{setBusy(false)}};return <main className="min-h-dvh bg-background px-4 py-10 sm:px-8"><div className="mx-auto max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">AI advisor</p><h1 className="mt-2 text-3xl font-bold">Ask GhanaPathFinder AI</h1><p className="mt-2 text-sm text-muted-foreground">Ask about education, careers, scholarships, opportunities or your next step.</p><div className="mt-8 rounded-2xl border border-border bg-card p-5"><label htmlFor="question" className="text-sm font-medium">Your question</label><textarea id="question" value={question} onChange={e=>setQuestion(e.target.value)} rows={5} className="mt-3 w-full rounded-xl border border-border bg-background p-3 outline-none" placeholder="What should I study if I want to work in AI and robotics?"/><button disabled={busy||!question.trim()} onClick={ask} className="mt-3 rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground disabled:opacity-50">{busy?"Thinking...":"Ask AI"}</button>{answer&&<div className="mt-6 whitespace-pre-wrap rounded-xl bg-secondary p-4 text-sm leading-6">{answer}</div>}</div><Link className="mt-5 inline-block text-sm text-primary" to="/explore">Explore the platform</Link></div></main>}
