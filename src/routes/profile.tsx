import { createFileRoute, Link } from "@tanstack/react-router";
import { useAuth } from "@/hooks/useAuth";
export const Route=createFileRoute("/profile")({component:Profile});
function Profile(){const {user,loading}=useAuth();return <main className="min-h-dvh bg-background px-4 py-10 sm:px-8"><div className="mx-auto max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Profile</p><h1 className="mt-2 text-3xl font-bold">Your GhanaPathFinder profile</h1>{loading?<p className="mt-6 text-sm text-muted-foreground">Loading profile...</p>:!user?<div className="mt-6 rounded-xl border border-border p-6"><p className="text-sm text-muted-foreground">Sign in to manage your profile.</p><Link className="mt-4 inline-block text-sm text-primary" to="/auth">Sign in</Link></div>:<div className="mt-6 grid gap-4 sm:grid-cols-2"><Link to="/onboarding" className="rounded-xl border border-border bg-card p-5 hover:bg-secondary"><h2 className="font-semibold">Complete onboarding</h2><p className="mt-2 text-sm text-muted-foreground">Update your role, education, skills and career direction.</p></Link><Link to="/preferences" className="rounded-xl border border-border bg-card p-5 hover:bg-secondary"><h2 className="font-semibold">Preferences and visibility</h2><p className="mt-2 text-sm text-muted-foreground">Control discovery and opportunity preferences.</p></Link><Link to="/international-students" className="rounded-xl border border-border bg-card p-5 hover:bg-secondary"><h2 className="font-semibold">International student directory</h2><p className="mt-2 text-sm text-muted-foreground">Opt in to discoverability when ready.</p></Link><Link to="/ai" className="rounded-xl border border-border bg-card p-5 hover:bg-secondary"><h2 className="font-semibold">AI profile advisor</h2><p className="mt-2 text-sm text-muted-foreground">Get guidance grounded in your profile and platform data.</p></Link></div>}</div></main>;}
