import { createFileRoute } from "@tanstack/react-router";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import OpportunityGraph from "@/components/OpportunityGraph";
import { OPPORTUNITY_CATEGORIES } from "@/lib/opportunity-categories";
export const Route=createFileRoute("/opportunities")({component:OpportunityHub});
function OpportunityHub(){return <div className="min-h-screen bg-background"><Navbar/><main className="pt-20 pb-14 px-4 sm:px-8"><div className="max-w-6xl mx-auto"><p className="text-xs uppercase tracking-wide text-primary font-semibold">Year-round opportunity</p><h1 className="font-display text-3xl font-bold mt-1">What can you do next?</h1><p className="text-sm text-muted-foreground mt-2 max-w-2xl">Jobs, internships, skills, funding, startups, research and community opportunities. Existing records only.</p><div className="mt-6"><OpportunityGraph/></div><div className="mt-8 grid sm:grid-cols-2 lg:grid-cols-3 gap-3">{OPPORTUNITY_CATEGORIES.map(c=><a key={c.slug} href={"/opportunities/"+c.slug} className="border border-border bg-glass rounded-xl p-4 hover:border-primary/40"><p className="text-sm font-semibold">{c.label}</p><p className="text-xs text-muted-foreground mt-1">{c.description}</p></a>)}</div></div></main><Footer/></div>}