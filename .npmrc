# Vercel runs `npm install`; next-themes@0.3.0 declares a React ^16–^18 peer
# range which conflicts with this project's React 19. Allow npm to resolve with
# legacy peer dependency handling (bun installs are unaffected).
legacy-peer-deps=true
